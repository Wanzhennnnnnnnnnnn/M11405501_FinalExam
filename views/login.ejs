<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - SRB System</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body { background-color: #f3f4f6; display: flex; justify-content: center; align-items: center; height: 100vh; }
        .auth-card { width: 100%; max-width: 400px; background: white; padding: 2rem; border-radius: 8px; box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1); border-top: 4px solid #6a6a9d; }
    </style>
</head>
<body>
    <div class="auth-card">
        <div class="text-center mb-6">
            <h1 class="text-2xl font-bold text-gray-800"><i class="fas fa-database mr-2" style="color: #6a6a9d;"></i>SRB System</h1>
            <p class="text-gray-500 text-sm mt-1">Please login to continue</p>
        </div>

        <% if (error) { %>
            <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4 text-sm">
                <%= error %>
            </div>
        <% } %>
        <% if (success) { %>
            <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4 text-sm">
                <%= success %>
            </div>
        <% } %>

        <form action="/login" method="POST">
            <div class="mb-4">
                <label class="block text-gray-700 text-sm font-bold mb-2">Email</label>
                <input type="email" name="email" class="input-box" required placeholder="admin@example.com">
            </div>
            <div class="mb-6">
                <label class="block text-gray-700 text-sm font-bold mb-2">Password</label>
                <input type="password" name="password" class="input-box" required placeholder="********">
            </div>
            <button type="submit" class="btn-submit w-full py-2 text-base shadow-md">Sign In</button>
        </form>

        <div class="mt-4 text-center text-sm flex justify-between">
            <a href="/register" class="text-blue-600 hover:text-blue-800 hover:underline">Create Account</a>
            <a href="/forgot-password" class="text-gray-500 hover:text-gray-700">Forgot Password?</a>
        </div>
    </div>
</body>
</html>