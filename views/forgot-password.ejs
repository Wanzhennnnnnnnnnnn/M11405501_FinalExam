<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Password - SRB System</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body { background-color: #f3f4f6; display: flex; justify-content: center; align-items: center; height: 100vh; }
        .auth-card { width: 100%; max-width: 400px; background: white; padding: 2rem; border-radius: 8px; box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1); border-top: 4px solid #6a6a9d; }
    </style>
</head>
<body>
    <div class="auth-card">
        <div class="text-center mb-6">
            <h1 class="text-2xl font-bold text-gray-800">Reset Password</h1>
            <p class="text-gray-500 text-sm mt-1">Enter your email to reset</p>
        </div>

        <% if (error) { %>
            <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4 text-sm">
                <%= error %>
            </div>
        <% } %>

        <form action="/forgot-password" method="POST">
            <div class="mb-4">
                <label class="block text-gray-700 text-sm font-bold mb-2">Registered Email</label>
                <input type="email" name="email" class="input-box" required placeholder="email@example.com">
            </div>
            <div class="mb-6">
                <label class="block text-gray-700 text-sm font-bold mb-2">New Password</label>
                <input type="password" name="new_password" class="input-box" required placeholder="New password">
            </div>
            <button type="submit" class="btn-submit w-full py-2 text-base shadow-md">Reset Password</button>
        </form>

        <div class="mt-4 text-center text-sm">
            Remembered? <a href="/login" class="text-blue-600 hover:text-blue-800 hover:underline">Back to Login</a>
        </div>
    </div>
</body>
</html>